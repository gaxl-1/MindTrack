server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://db:5432/mindtrack
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
jwt:
  secret: your-256-bit-secret-key-replace-this # Cambiar por clave segura
  expiration: 86400000 # 24h en ms